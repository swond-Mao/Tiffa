---
description: "Code blocks must specify a language, e.g. ```python not just ```"
condition: "(^|\\n)```\\s*\\n"
scope: "text"
interruptMode: "always"
---

You opened a code block without specifying a language. Every code block MUST have a language tag.
Write ```python, ```typescript, ```bash, etc. — never just ``` with no language.
