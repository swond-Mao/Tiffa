# 四重记忆架构概览

<cite>
**本文引用的文件**   
- [README.md](file://README.md)
- [AGENTS.md](file://AGENTS.md)
- [config.yml](file://data/agent/config.yml)
- [main.js](file://electron/main.js)
</cite>

## 目录
1. [引言](#引言)
2. [项目结构](#项目结构)
3. [核心组件](#核心组件)
4. [架构总览](#架构总览)
5. [详细组件分析](#详细组件分析)
6. [依赖关系分析](#依赖关系分析)
7. [性能考量](#性能考量)
8. [故障排查指南](#故障排查指南)
9. [结论](#结论)
10. [附录](#附录)

## 引言
本文件面向 Tiffa 的四重记忆系统，系统性阐述 L1 USER.md（用户偏好）、L2 MEMORY.md（全局事实）、L3 PROJECT.md（项目上下文）与 L4 Mnemopi 语义记忆的分层设计理念、职责分工、存储位置、访问优先级、注入时机与性能特征。同时解释“零延迟注入”机制的设计原理，以及四层记忆如何协同工作为智能体提供完整上下文支持。文档包含记忆层次图与数据流向说明，帮助读者快速理解并正确使用该体系。

## 项目结构
Tiffa 的记忆系统由“确定性注入 + 语义检索”双通道组成：
- 确定性注入：USER.md、MEMORY.md、PROJECT.md 在会话或任务关键阶段被即时读取并注入，不占用额外 token 的检索开销，实现“零延迟”。
- 语义检索：Mnemopi 作为全局 RAG 库，自动累积对话片段，按需召回并限制注入长度，避免上下文爆炸。

```mermaid
graph TB
subgraph "桌面端"
UI["Electron 前端"]
Main["主进程 main.js"]
end
subgraph "Agent 内核"
Agent["pi-coding-agent"]
Hooks["Hooks 链<br/>before_agent_start / session.compacting"]
Rules["TTSR 规则"]
end
subgraph "记忆层"
L1["L1 USER.md<br/>用户偏好"]
L2["L2 MEMORY.md<br/>全局事实"]
L3["L3 PROJECT.md<br/>项目上下文"]
L4["L4 Mnemopi<br/>语义记忆(RAG)"]
end
UI --> Main
Main --> Agent
Agent --> Hooks
Hooks --> L1
Hooks --> L2
Hooks --> L3
Agent --> L4
Rules --> Agent
```

图表来源 
- [main.js:240-278](file://electron/main.js#L240-L278)
- [AGENTS.md:113-156](file://AGENTS.md#L113-L156)

章节来源
- [README.md:45-74](file://README.md#L45-L74)
- [AGENTS.md:61-77](file://AGENTS.md#L61-L77)

## 核心组件
- L1 USER.md（用户偏好）
  - 存储位置：工作区根目录 USER.md
  - 职责：记录用户身份、语言、量化偏好等个性化信息
  - 注入时机：每轮对话前（zero-latency），确保每次推理都具备用户画像
  - 访问优先级：最高（首轮必读）
  - 性能特征：纯文本直读，无网络与模型加载，毫秒级

- L2 MEMORY.md（全局事实）
  - 存储位置：工作区根目录 MEMORY.md
  - 职责：跨项目共享的全局事实索引（如硬件环境、服务管理方式）
  - 注入时机：会话初始化时注入，跨项目共享
  - 访问优先级：高（紧随 USER.md）
  - 性能特征：纯文本直读，控制条目数量（建议 30 条以内）

- L3 PROJECT.md（项目上下文）
  - 存储位置：当前项目根目录 PROJECT.md
  - 职责：当前项目的架构、决策、约束与规范
  - 注入时机：before_agent_start 钩子中生成/更新并注入；按 cwd 隔离
  - 访问优先级：中高（项目级确定性上下文）
  - 性能特征：首次生成可能耗时，后续增量更新；注入上限受控

- L4 Mnemopi（语义记忆）
  - 存储位置：全局向量库（scoping=global），metadata.cwd 标记来源
  - 职责：全量积累对话片段，提供跨项目语义检索与断片恢复
  - 注入时机：autoRetain 每 N 轮写入；manual recall 或 gap-fill 时注入
  - 访问优先级：按需召回，受 recallLimit 与 injectionTokenLimit 限制
  - 性能特征：embedding 模型本地运行；冷启动预热可避免首次超时

章节来源
- [README.md:45-74](file://README.md#L45-L74)
- [AGENTS.md:275-310](file://AGENTS.md#L275-L310)
- [config.yml:13-27](file://data/agent/config.yml#L13-L27)

## 架构总览
四重记忆通过“确定性注入 + 语义检索”协作，形成低延迟、强一致、可扩展的上下文供给体系。

```mermaid
sequenceDiagram
participant U as "用户"
participant E as "Electron 前端"
participant M as "主进程(main.js)"
participant A as "Agent 内核"
participant H as "Hooks(before_agent_start/session.compacting)"
participant L1 as "L1 USER.md"
participant L2 as "L2 MEMORY.md"
participant L3 as "L3 PROJECT.md"
participant L4 as "L4 Mnemopi"
U->>E : 发送消息
E->>M : IPC 转发
M->>A : 启动/唤醒实例
A->>H : before_agent_start
H->>L1 : 读取 USER.md
H->>L2 : 读取 MEMORY.md
H->>L3 : 读取/生成 PROJECT.md
A->>L4 : autoRetain(每N轮)
A-->>H : 压缩触发 session.compacting
H->>L4 : gap-fill 提取并立即注入
A-->>M : 返回结果
M-->>E : 渲染输出
```

图表来源 
- [main.js:240-278](file://electron/main.js#L240-L278)
- [AGENTS.md:113-156](file://AGENTS.md#L113-L156)
- [AGENTS.md:275-310](file://AGENTS.md#L275-L310)

## 详细组件分析

### L1 USER.md（用户偏好）
- 设计目标：以最小成本提供稳定的用户画像，确保每次推理都“认识你”
- 存储与访问：工作区根目录，纯文本；每轮对话前读取
- 注入策略：确定性注入，零延迟，优先于其他记忆层
- 性能特征：无 I/O 阻塞风险（小文件），无模型调用，响应时间稳定

```mermaid
flowchart TD
Start(["开始"]) --> ReadUser["读取 USER.md"]
ReadUser --> Inject["注入到 systemPrompt 前缀"]
Inject --> Next["进入下一环节"]
Next --> End(["结束"])
```

章节来源
- [README.md:45-63](file://README.md#L45-L63)

### L2 MEMORY.md（全局事实）
- 设计目标：跨项目共享的关键事实索引，避免重复描述
- 存储与访问：工作区根目录，纯文本；会话初始化时注入
- 注入策略：确定性注入，控制条目规模，避免上下文膨胀
- 性能特征：轻量直读，适合高频复用

章节来源
- [README.md:54-63](file://README.md#L54-L63)

### L3 PROJECT.md（项目上下文）
- 设计目标：为当前项目提供结构化、可演进的上下文（架构、决策、约束）
- 存储与访问：项目根目录，按 cwd 隔离；首次对话自动生成脚手架
- 注入策略：before_agent_start 钩子中生成/更新并注入；确定性注入
- 性能特征：首次生成可能较慢，后续增量更新；注入上限受控

```mermaid
sequenceDiagram
participant Hook as "before_agent_start"
participant FS as "文件系统"
participant P as "PROJECT.md"
Hook->>FS : 检查是否存在 PROJECT.md
alt 不存在
Hook->>P : 生成脚手架
else 存在
Hook->>P : 增量更新
end
Hook-->>Hook : 注入 systemPrompt
```

图表来源 
- [AGENTS.md:113-156](file://AGENTS.md#L113-L156)

章节来源
- [AGENTS.md:113-156](file://AGENTS.md#L113-L156)

### L4 Mnemopi（语义记忆）
- 设计目标：全量积累对话片段，提供跨项目语义检索与断片恢复
- 存储与访问：全局向量库（scoping=global），metadata.cwd 标记来源
- 注入策略：
  - autoRetain：每 N 轮写入一次
  - manual recall：按需检索，最多 recallLimit 条
  - gap-fill：压缩时提取关键点并立即注入
- 性能特征：embedding 模型本地运行；冷启动预热避免首次超时；注入上限 2000 token

```mermaid
flowchart TD
Start(["会话事件"]) --> Retain{"是否达到 retainEveryNTurns?"}
Retain --> |是| Embed["生成 embedding 并入库"]
Retain --> |否| Wait["等待下一轮"]
Embed --> Recall{"是否需要 recall?"}
Recall --> |是| Search["语义检索并排序"]
Search --> Limit["限制注入 token 数"]
Limit --> Inject["注入到上下文"]
Recall --> |否| End(["结束"])
Inject --> End
```

图表来源 
- [config.yml:13-27](file://data/agent/config.yml#L13-L27)
- [AGENTS.md:275-310](file://AGENTS.md#L275-L310)

章节来源
- [config.yml:13-27](file://data/agent/config.yml#L13-L27)
- [AGENTS.md:275-310](file://AGENTS.md#L275-L310)

### 零延迟注入机制
- 设计原理：将最关键的三层记忆（USER/MEMORY/PROJECT）以确定性方式在关键钩子中直接读取并注入，避免检索与模型加载带来的延迟
- 触发点：
  - before_agent_start：注入 USER/MEMORY/PROJECT
  - session.compacting：gap-fill 立即注入，不等下轮
- 效果：首句即可获得完整上下文，提升弱模型可用性

```mermaid
sequenceDiagram
participant S as "会话开始"
participant H as "before_agent_start"
participant G as "session.compacting"
participant C as "上下文"
S->>H : 触发
H->>C : 注入 USER/MEMORY/PROJECT
S->>G : 压缩触发
G->>C : 注入 gap-fill 要点
C-->>S : 完成
```

图表来源 
- [AGENTS.md:113-156](file://AGENTS.md#L113-L156)

章节来源
- [AGENTS.md:113-156](file://AGENTS.md#L113-L156)

## 依赖关系分析
- Electron 主进程负责实例生命周期管理与预热（embedding 模型加载）
- Agent 内核通过 Hooks 链协调记忆注入与工具调用拦截
- 记忆层分为确定性注入（L1/L2/L3）与语义检索（L4），两者互补

```mermaid
graph LR
Main["main.js"] --> Agent["Agent 内核"]
Agent --> Hooks["Hooks 链"]
Hooks --> L1["L1 USER.md"]
Hooks --> L2["L2 MEMORY.md"]
Hooks --> L3["L3 PROJECT.md"]
Agent --> L4["L4 Mnemopi"]
```

图表来源 
- [main.js:240-278](file://electron/main.js#L240-L278)
- [AGENTS.md:113-156](file://AGENTS.md#L113-L156)

章节来源
- [main.js:240-278](file://electron/main.js#L240-L278)
- [AGENTS.md:113-156](file://AGENTS.md#L113-L156)

## 性能考量
- 零延迟注入：L1/L2/L3 为纯文本直读，无检索与模型加载，响应时间稳定
- 语义检索：L4 使用本地 embedding 模型，需预热以避免首次冷加载超时
- 注入上限：L4 注入 token 上限 2000，防止上下文爆炸
- 预热策略：主进程在就绪后延迟触发 /memory rebuild，提前加载 embedding 模型

章节来源
- [main.js:240-278](file://electron/main.js#L240-L278)
- [config.yml:13-27](file://data/agent/config.yml#L13-L27)

## 故障排查指南
- 首次消息超时或失败
  - 现象：embedding 冷加载导致超时
  - 处理：确认主进程已执行 /memory rebuild 预热；检查 embedding 模型路径与权限
- 记忆未注入
  - 现象：USER/MEMORY/PROJECT 未生效
  - 处理：检查 before_agent_start 钩子是否触发；确认文件存在且可读
- 注入内容过多
  - 现象：上下文过长影响弱模型
  - 处理：调整 recallLimit 与 injectionTokenLimit；精简 MEMORY.md 条目

章节来源
- [main.js:240-278](file://electron/main.js#L240-L278)
- [AGENTS.md:275-310](file://AGENTS.md#L275-L310)

## 结论
Tiffa 的四重记忆系统通过“确定性注入 + 语义检索”的双通道设计，实现了低延迟、强一致、可扩展的上下文供给。L1/L2/L3 保障关键信息的即时可用，L4 提供跨项目语义检索与断片恢复能力。配合主进程的预热策略与注入上限控制，整体系统在弱模型场景下依然表现稳健。

## 附录
- 配置参考：mnemopi 参数通过 settings 数据库写入，config.yml 嵌套写法不生效
- 技能加载：通过 read skill://<name> 协议加载 SKILL.md
- 环境变量：PI_CODING_AGENT_DIR、HOME/USERPROFILE、KIMI_API_KEY、PORTABLE_ROOT

章节来源
- [AGENTS.md:61-77](file://AGENTS.md#L61-L77)
- [AGENTS.md:144-156](file://AGENTS.md#L144-L156)
- [AGENTS.md:256-264](file://AGENTS.md#L256-L264)