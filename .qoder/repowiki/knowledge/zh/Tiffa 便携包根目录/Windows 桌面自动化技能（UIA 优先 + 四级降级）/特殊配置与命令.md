依赖通过 `requirements.txt` 管理（mss/Pillow/pyautogui），MCP 模式需先启动外部 MCP 客户端进程并通过 JSON-RPC 调用；独立运行使用 `python computer_use.py run "任务" --api-base http://127.0.0.1:11434/v1 --model I-Compact`，支持 `--force` 跳过 MessageBox 确认。