各 skill 依赖安装方式不同：`diagram-drawing` 需 `pip install -r scripts/requirements.txt && python -m playwright install chromium`；`pdf` 用 `bash scripts/make.sh check/run/reformat`；`xlsx` 需 LibreOffice 并通过 `python recalc.py` 重算公式；`history-query` 直连 `D:\AI\Opencode\data\share\opencode\opencode.db`；`dashiai-ppt` Windows 上需用 `npm --prefix project run preview:https` 替代 Unix 的 `preview:start`。