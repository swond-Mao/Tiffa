安装：双击 `install.bat`（内部调用 `install.ps1`）。运行：`tiffa-desktop.exe` 或 `start-desktop.bat` 启动 GUI；`start-tiffa.bat --tui/--web/--rpc` 启动终端/WebUI/RPC 模式；开发模式 `cd electron && npm start`。所有路径通过 `--portable-root` 参数或 `PORTABLE_ROOT` 环境变量指向当前目录，无需全局安装。