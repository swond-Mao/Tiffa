开发运行：`npm start`（即 `electron .`），加 `--dev` 或 `--verbose` 打开 DevTools；调试模式：`npm run dev`（等价于 `electron . --dev`）；构建便携包：`npm run build`（输出到 `../dist/tiffa-portable-<version>.exe`）。依赖外部目录结构：`npm-global/`（含 bun 和 pi-coding-agent）、`plugins/`、`data/`、`home/`、`workspace/` 通过 `extraResources` 打包进应用。