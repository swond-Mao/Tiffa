Electron 33 + Bun 1.3.14（运行 Tiffa 内核）、`js-yaml`/`yaml`（models.yml 配置）、`marked` + `highlight.js`（Markdown 代码高亮）、`electron-builder` 打包为 Windows 便携版（`--win portable`），asar 禁用以便直接读写 `data/plugins/home/workspace` 目录。