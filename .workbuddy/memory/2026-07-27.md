# 2026-07-27

- 确立 Tiffa 复杂任务设计文档纪律（Layer 2 行为约束，落在 `data/memory/constraints-inject.md` 的「先计划再执行」）：
  - 新建全局纲要 `data/memory/design-outline.md`（规范源，按需读取，避免每轮注入爆炸上下文）。
  - 升级原"先计划再执行"为指针版：复杂任务先读纲要，按结构在 `<当前项目>/design/<工作方向>-design.md` 产出设计稿，用户确认后执行，执行以文档为准、恢复时重读。
  - 命名：纲要全局唯一 `design-outline.md`；设计稿 `<工作方向>-design.md` 进各项目 `design/` 子目录（非 workspace 根），同项目多次设计不覆盖。
  - 动机：用户观察 Qoder"复杂任务先写技术设计文档"习惯，判定其为框架工作纪律（Layer 2 事前注入）而非 TTSR（Layer 1 流式拦截/事中纠错）。

- **救援 Tiffa 启动失败**（外部项目隔离重构把自己改挂）：
  - 根因：Tiffa 改 `electron/main.js` 重构 `cleanupProjectsJson()` 时，误删上层 `ensureProjectInJson` 的收尾三行（`}` 闭合 else 块 + `return normalized;` + `}` 闭合函数），导致全文件大括号不配平 → `node --check` 报 "Unexpected end of input" @ EOF，Electron 主进程启动即崩。
  - 第二处：`electron/preload.js` 加 `removeSuspendedProject` 时把 `listArchivedProjects: () => ...` 的右值弄丢，只剩 `listArchivedProjects:` → 对象字面量语法错。
  - 修复：①main.js 在 `ensureProjectInJson` 末尾补回那三行；②preload.js 补回 `listArchivedProjects: () => ipcRenderer.invoke('sessions:listArchived'),`。
  - 校验：main.js / preload.js / app.js `node --check` 全过；config.yml 用 js-yaml 解析正常。其余改动（styles.css、config.yml 去引号+顶层 mnemopi 块）未引入启动错误。
  - 排查手法：`git diff` 定位 Tiffa 自改内容 + `node --check` 逐文件语法校验。外部项目隔离功能逻辑本身未回退，仅恢复启动。

- **修正设计稿落盘位置（用户指出放错文件夹）**：Tiffa 按纪律生成的设计稿 `cross-device-project-handling-design.md` 落在了 `E:\Tiffa\design\`（应用根目录），用户判定应为实际项目文件夹。已 `mv` 至 `E:\Tiffa\workspace\Tiffa开发\design\` 并删除空的应用根 `design/` 目录。同步在 `design-outline.md` 与 `constraints-inject.md`「先计划再执行」补一句："当前项目指实际处理的项目目录（如 `workspace/Tiffa开发`），不要放在 Tiffa 应用根目录 `E:\Tiffa` 下"，防止复发。

- **改用 git 恢复而非手动补括号**：Tiffa 改坏后虽手动补括号让语法通过，但运行时仍"打不开"（点击静默失败，无前端报错，疑似 config.yml / app.js 隐藏逻辑坑）。用户指出应直接 `git checkout` 恢复。执行 `git checkout --` 回退 Tiffa 在外部项目隔离任务中改坏的 7 个 tracked 文件（main.js / preload.js / app.js / styles.css / config.yml / start-desktop.bat[曾被误删] / 技术开发文档.docx）到干净 commit `83eba3f`；恢复后三核心文件 `node --check` 全过，工作区仅剩 untracked 产物。外部项目隔离功能随之被撤（设计文档 `workspace/Tiffa开发/design/cross-device-project-handling-design.md` 保留，可据其重做）。教训：agent 改 Electron main.js 易自毁，优先 git 恢复一步到位，别绕路手补。
