# 2026-08-02

## 技术开发文档重写（开发文档.md v2）

把 `G:/Tiffa/开发文档.md` 从 197 行重写为 443 行，对齐 commit `163d3d3` 的真实代码状态。**所有数字都是现场核实的，不是照抄旧文档。**

### 发现的文档漂移（旧文档 → 实测）

| 项 | 旧值 | 实测 |
|---|---|---|
| IPC handler | 37 | **55** |
| TTSR 规则 | 10 | **13** |
| skills | 18 | **19** |
| main.js | 1789（AGENTS.md） | **2729** |
| app.js | ~5300 | **5896** |
| styles.css | 2846（AGENTS.md） | **3933** |

### 两处必须记住的「文档撒谎」

1. **AGENTS.md 的 Mnemopi 配置是错的**。AGENTS.md 写 `scoping=global` / `autoRecall=false`，
   但 `agent.db` 的 settings 表实测是 **`per-project-tagged` / `autoRecall=true`**（还有 `debug=true`）。
   `开发文档.md` 原来写的才对。核实方法：
   ```
   npm-global/node_modules/bun/bin/bun.exe -e "const {Database}=require('bun:sqlite');
   const db=new Database('G:/Tiffa/data/agent/agent.db',{readonly:true});
   console.log(db.query(\"SELECT key,value FROM settings WHERE key LIKE '%mnemopi%'\").all())"
   ```
2. **`claude-mode-extension.ts` 文件头注释是过期的**。头注释把 gap-fill 列进「已删除 → Mnemopi autoRecall 覆盖」，
   但代码里 gap-fill 完整存在：22 处引用 + `session.compacting` hook 在 line 575 注册，
   照常写 `compact-*.txt` dump 和 `gap-fill-*.md`。**信代码不信注释。**

### 新增章节

- 品牌图标构建管线（7 尺寸 ICO 手工拼装 + resedit 给无图标 exe 新增图标组，两个坑）
- `followScroll` 三态机（smooth 与流式 scrollTop 互斥）
- 模型白名单 `undefined` 语义
- Computer Use v3（8 个 MCP 工具 / 三阶段强制流程 / 五级降级 L1-L5，含新增的 L5 OCR）
- 本地 provider 命名铁律（WebP 静默崩 + supportsTools 协议切换 + 验证陷阱）
- Git 双端推送的 wincred 绕过法
- 实例生命周期修复要点 + 会话删除必须关实例

### 待办（已于同日完成，见下）

- ~~AGENTS.md 的过期数字与错误 Mnemopi 配置尚未同步~~
- ~~`tiffa-技术开发文档.docx`（7/27）已严重落后于 md 版~~

## AGENTS.md 校正为 v7 + docx 重新导出

用户授权后完成两件收尾：

1. **AGENTS.md 重写为 v7（318 → 约 340 行）**，全部数字现场核实：
   - Mnemopi 配置改正为 `per-project-tagged` / `autoRecall=true` / `debug=true`（原写 `global` / `false`，是错的），并合并原来重复的两个记忆章节为一节 + 五层分层表。
   - TTSR 10 → **13 条**（补 `intermediate-files-to-temp` / `no-direct-mnemopi-inspection` / `no-repeated-tool-calls`）。
   - skills 18 → **19**（补 `computer-use`），新增 Computer Use v3 速查（8 工具 / 三阶段 / 五级降级）。
   - IPC 37 → **55**（按 6 组给数量与代表）；`MAX_INSTANCES` 5 → **8**；崩溃重启改为「任何非 userKilled 退出含 code===0，最多 3 次」；显式标注 `forceKill` 是死代码、旧的「stall → forceKill」不存在。
   - 行数全部更新：main.js 2729 / app.js 5896 / styles.css 3933 / themes.js 675 / 扩展 779 / computer-use-extension 128。
   - 扩展章节加警示：**文件头注释声称 gap-fill 已删除是过期的，以代码为准**；hook 顺序按代码实际（compacting 在 session_stop 之前）。
   - 新增：品牌图标路径、窗口尺寸 1600×1000 / 1100×720、followScroll 三态机、模型白名单 undefined 语义、会话删除必须关实例、Git 双端 wincred 推送法、环境变量补 `MNEMOPI_EMBEDDING_MODEL`。
   - 保留原有的 ComfyUI 速查、Windows 开发注意事项、技能加载方式（`read skill://`）、/omfg。

2. **`tiffa-技术开发文档.docx` 由 `开发文档.md` 重新导出**：
   ```bash
   "/c/Program Files/Pandoc/pandoc" 开发文档.md -o tiffa-技术开发文档.docx \
     --toc --toc-depth=2 -f gfm -t docx --metadata toc-title=目录
   ```
   - 本机 **pandoc 在 `C:\Program Files\Pandoc\pandoc.exe`**，可直接用，不需要 python-docx（venv 里也没装）。
   - `--metadata lang=zh-CN` 会报 `Could not load translations for zh-CN`，只是缺 zh.yaml 词条（Abstract/Table 标签），不影响正文；直接用 `toc-title=目录` 更省事。
   - 产物 19163 → 28018 字节，11 个表格、39 个标题、12152 字符，关键词（per-project-tagged / tiffa-icon.ico / wincred / Computer Use v3 / followScroll）均已核验存在。
   - Word 打开时目录字段需右键「更新域」才显示页码。

**未提交 git**（用户未要求），改动文件：`AGENTS.md`、`tiffa-技术开发文档.docx`。
