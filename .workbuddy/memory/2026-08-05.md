# 2026-08-05 工作日志

## embedding 模型分布排查（用户装机问询）

- 用户想「git clone + install 从国内源拉依赖 + 可选安装位置」自包含装机，担心 embedding 模型国内无源。
- 查清：真正运行的 embedding 是 `home/.omp/cache/fastembed/fast-bge-small-zh-v1.5`（93MB，BAAI/bge-small-zh-v1.5 512d）+ `home/.omp/cache/fastembed-runtime/fastembed-2.1.0_transitive-ort`（onnxruntime 绑定），都在 gitignore 的 `home/` 下 → 不进 git。
- `.gitignore` 里 force 保留的 `data/agent/mnemopi/` 4 文件是死配置（目录本机不存在）；`local_cache/fast-bge-small-zh-v1.5`(1.3MB 仅 tokenizer) 是误导项。
- `node/`(694M) `python/`(2.3G) `npm-global/` 全 gitignore；`install.ps1` 只补 bun+内核(npmmirror)，不装 node/python。
- 结论：当前 git clone+install 不自包含；embedding 必须随包（LFS 或便携拷贝）。已纠正 MEMORY.md「分发与装机」段错误描述。
- 待用户决策分发方案（A install bootstrap+embedding LFS / B 全 LFS / C 源码+单独运行时包）及是否加交互式安装路径选择。

## 方案定案（用户确认「能拉的部分拉，不能拉的 LFS」）

- 关键核查：`python/tesseract/` 是**空目录**、全代码不调用 tesseract；真正 OCR 走 `winrt-Windows.Media.Ocr`（Windows 原生）。故 Python 环境 100% pip 可重建，无必须 LFS 的非 pip 组件。
- 最终设计：① embedding 权重(94MB) → **Git LFS**，源 `embedding-assets/fast-bge-small-zh-v1.5/`（绕开 `home/` 忽略链），install 拷到 `home/.omp/cache/fastembed/`；② Node v22.17.1 → install 从 npmmirror binary 下 zip 解压；③ Python 3.13.12 → install 下 embeddable + ensurepip/get-pip(清华) + `pip install -r requirements-python.txt`(清华)；④ fastembed-runtime(870MB) → 首次启用记忆时内核 bun 从 npmmirror 自动拉（根 `.npmrc` 写国内源），失败则提示从源机拷贝；⑤ bun+内核 维持 npmmirror。
- 落地改动：`install.ps1`（node/python 自举 + embedding 拷贝 + 缺失提示，语法校验通过）、`.gitattributes`（LFS）、新增 `embedding-assets/`、`requirements-python.txt`、根 `.npmrc`（install 写）。
- 验证：npmmirror node/python 二进制 302→200 可达；清华 pip 200；git lfs 3.7.1 可用；embedding 7 文件已转 LFS 指针暂存。
- 待办：用户需在干净机器实测 install（尤其 Python embeddable+pip 重建、runtime 自动拉）；push 时用 `git lfs push`（确认 gitee/gitcode LFS 额度）。

## 文档对齐 + 推送（用户：记忆改造革新/claude化落地，更新技术开发文档与两仓库 README）

- **技术开发文档 docx（v2→v3）**：python-docx 1.2.0 改。章节「三、五层记忆系统」→「三、记忆系统（L1–L4 分层 + 压缩摘要兜底）」；L5 行标记「2026-08-05 已废弃」；删 gap-fill 四步流程，改为压缩路由 4 级（①本地视觉→②主模型视觉→③旁路结构化总结→④内核自压）；Hook#3 改为压缩路由描述；删「文件头注释声称 gap-fill 已删除（过期注释）」那段（现已真删）；Claude 化扩展 779→1063 行；硬编码 `G:\Tiffa`→`$ROOT`；版本号/commit 更新。
- **README.md**：五重→四重记忆（L5 废弃+压缩摘要说明）、Mnemopi 断片补救改述、TTSR「10 条」→「13 条」并补全 3 条规则（intermediate-files-to-temp / no-direct-mnemopi-inspection / no-repeated-tool-calls）、`constraints.md`→`constraints-inject.md`、弱模型增强表 gap-fill 行改「压缩摘要」、新增「Claude 化扩展」章节（6 Hook + 旁路总结 + 熔断 + /omfg）。
- 提交 `e3019fab`：含上述文档 + 前次 staged 的 install.ps1/embedding/requirements/MEMORY.md + 新技能 `skills/video-prompt-gen/`。**刻意排除**：`data/agent/*` 运行时状态、含公网 IP 的 `bypass-model.json`、`.workbuddy/memory` 日记、`.workbuddy/temp`。
- **推送踩坑（重要）**：gitee 免费版**不支持 LFS**（报 paid/trial only）→ embedding 无法走 LFS；gitcode LFS 传 90MB 还被远端强制断连。故**放弃 LFS，embedding 改普通文件直接提交**（HEAD 中是真实 90MB blob，无 pointer）。`git push origin master` 成功（`075241ce..e3019fab`，gitee 接受 90MB 普通文件，约 20 分钟）；`git push gitcode` 仍在传 90MB（慢，后台 Ds58Fp 进行中，待通知）。已同步更新 MEMORY.md「分发与装机」段：gitee 不支持 LFS、embedding 走普通文件、git push 传大 pack 较慢但可用。
- 注意：本会话 `last-compact-summary.md`/`bypass-model.json` 等运行时文件仍在工作区未提交（含对话内容与公网 IP），保持不提交。

## README 补 Computer Use 卖点（用户：windows use 也算卖点）

- README 对比表新增「操作电脑（桌面自动化）」行；新增「操作电脑（Computer Use）」独立章节：原子工具集(ui_inspect/ui_act/ui_screenshot/desktop_input/computer_use) + 主模型驱动(v2 删内嵌 VLM 黑盒) + 四级降级(L1 UIA Pattern→L2 坐标→L3 SoM→L4 归一化坐标) + 工程细节(DPI 感知/截图缩放/返回值带图验证) + 默认豆包 doubao-seed；并诚实标注「不算特别好用、复杂界面仍失手」。技术开发文档 docx 已有 Computer Use 完整技术章节（五级降级等），未重复改。
- 提交 `dca60c8f`，推 gitee + gitcode 双远端均成功（`e3019fab..dca60c8f`）。推送中 `geometric repack` 为 gc 优化非致命警告，不影响结果。
