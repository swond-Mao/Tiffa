# Tiffa 技术开发文档

> 基于 `@oh-my-pi/pi-coding-agent` v17.0.7 的便携 AI 工作台
> Electron 桌面壳 + Bun 内核子进程 + JSONL 协议

---

## 一、总体架构

```
┌──────────────────────────────────────────────────────────┐
│                    Electron 桌面壳                         │
│  main.js (主进程)          renderer/ (渲染进程)            │
│  ├─ TiffaInstanceManager   ├─ app.js (主逻辑 ~5300行)     │
│  ├─ 37个 IPC handler       ├─ index.html                 │
│  └─ 窗口/主题管理           ├─ styles.css (HSL主题)        │
│                            └─ themes.js (7套预设)         │
└──────────────┬───────────────────────────────────────────┘
               │ JSONL over stdin/stdout
┌──────────────▼───────────────────────────────────────────┐
│              Bun 内核子进程 (cli.js)                       │
│  ├─ Agent 核心 (工具调用/会话管理)                          │
│  ├─ Mnemopi 记忆引擎 (SQLite + ONNX embedding)            │
│  ├─ TTSR 流式规则引擎                                     │
│  └─ 扩展加载器 (-e plugins/claude-mode-extension.ts)      │
└──────────────────────────────────────────────────────────┘
```

**通信协议**：每行一个 JSON 对象（JSONL）。主进程向子进程 stdin 写命令帧，从 stdout 读事件帧。

---

## 二、关键路径

| 路径 | 说明 |
|------|------|
| `G:\Tiffa\` | 便携包根目录（`PORTABLE_ROOT`） |
| `npm-global/node_modules/@oh-my-pi/pi-coding-agent/` | 内核 |
| `npm-global/node_modules/bun/bin/bun.exe` | Bun 运行时 |
| `data/agent/` | 配置目录（config.yml, models.yml, agent.db） |
| `data/agent/rules/` | TTSR 规则目录 |
| `data/agent/memories/mnemopi/` | Mnemopi 记忆数据库 |
| `data/memory/` | USER.md / MEMORY.md / constraints-inject.md |
| `plugins/claude-mode-extension.ts` | Claude 化扩展 |
| `skills/` | 18 个专业技能 |
| `workspace/` | 工作区根目录 |

**环境变量**（由 main.js / start-tiffa.bat 注入）：

| 变量 | 值 | 说明 |
|------|---|------|
| `PORTABLE_ROOT` | `G:\Tiffa` | 便携根目录 |
| `PI_CODING_AGENT_DIR` | `$ROOT/data/agent` | Agent 数据目录 |
| `HOME` / `USERPROFILE` | `$ROOT/home` | 重定向到便携包内 |
| `MNEMOPI_EMBEDDING_MODEL` | `BAAI/bge-small-zh-v1.5` | **必须**，否则回退英文模型 |

---

## 三、五层记忆系统

| 层 | 载体 | 内容 | 生命周期 | 注入方式 |
|---|------|------|---------|---------|
| L1 | `data/memory/USER.md` | 用户身份/偏好 | 永久 | 每会话全文注入 |
| L2 | 全局 bank | 跨项目使用历史 | 永久积累 | 语义召回 |
| L3 | `workspace/<proj>/PROJECT.md` | 项目纲领/铁律 | 人工维护 | 每会话全文注入 |
| L4 | 项目 bank | 项目近期进度 | 自动积累+退化 | 语义召回（优先） |
| L5 | gap-fill | 本次对话瞬时上下文 | 60 分钟 | 压缩时即时注入 |

### Mnemopi 配置（settings 数据库）

```
mnemopi.embeddingModel      = BAAI/bge-small-zh-v1.5
mnemopi.scoping             = per-project-tagged   # 双层召回
mnemopi.autoRecall          = true                 # 首条消息自动召回
mnemopi.autoRetain          = true                 # 每2轮自动写入
mnemopi.retainEveryNTurns   = 2
mnemopi.recallLimit         = 10                   # 单次最多召回10条
mnemopi.injectionTokenLimit = 2000                 # 注入上限
```

**scoping 三种模式**：
- `global`：全部写全局 bank，只召回全局
- `per-project`：写项目 bank，只召回项目
- `per-project-tagged`（当前）：写项目 bank 打标签，召回时**并行搜项目+全局**，项目优先

**数据库位置**：`data/agent/memories/mnemopi/mnemopi.db`（全局 bank），项目 bank 在 `banks/` 子目录。

### 写入路由规则

| 用户说的话 | 写入位置 |
|-----------|---------|
| "这个项目必须/不能 xxx" | PROJECT.md（铁律） |
| "记录到项目记忆" | PROJECT.md |
| 项目目标/里程碑 | PROJECT.md（非必要不写） |
| "以后你都必须/不能 xxx" | USER.md（跨项目偏好） |
| 其他（踩坑/决策/进度） | mnemopi 自动积累 |

---

## 四、三重约束机制

```
第一重：TTSR 流式规则（零 Context 成本）
  data/agent/rules/*.md
  模型输出时实时正则匹配，违规立即拦截，不占 token

第二重：before_agent_start Hook（语义约束）
  data/memory/constraints-inject.md
  注入 system prompt 前缀：读文件规范/任务计划/skill铁律

第三重：tool_call Hook（危险操作拦截）
  危险路径 / 配置文件自改 / workspace 根目录新建 / 静默工具调用检测
```

**/omfg 命令**：`/omfg <抱怨>` → 自动生成 TTSR 规则文件，即时生效。

---

## 五、多实例管理（TiffaInstanceManager）

- 每个 `(cwd, sessionId)` 对应一个 Bun 子进程
- 最大实例数 8，LRU 淘汰（跳过 agentRunning 实例）
- 崩溃自动重启（最多 3 次，3 秒延迟）
- stall 检测：3 分钟无事件 → abort+steer；再 30 秒 → forceKill

**关键修复点**（多对话切换）：
1. `session_switch` 事件白名单透传（不被路由过滤）
2. `__new__` 会话的 sessionId 持久化到 session 对象
3. 预热过滤：用户命令立即取消 `isPrewarming`
4. 模型恢复：`ready` 事件优先从 sessionModelMap 恢复

---

## 六、启动流程（双阶段等待）

```
t=0s     遮罩显示 "静候枰开…"
t=2-5s   CLI ready → "凝心定神…"（等待引擎）
t=5-8s   /memory rebuild + embedding 加载 → "阅览旧谱…"
t=8-11s  遮罩淡出 → 入局
```

- 阶段 1：轮询 `isReady()`，最长 20 秒
- 阶段 2：固定等 6 秒（记忆系统预热），确保 autoRecall 就绪
- 遮罩期间输入禁用，防止消息丢失

---

## 七、开发调试

### 启动方式

```bat
tiffa-desktop.exe        # 双击启动 GUI
start-desktop.bat        # 带 --portable-root
start-tiffa.bat --tui    # 终端模式
start-tiffa.bat --web    # RPC-UI JSON 事件流
cd electron && npm start # 开发模式
```

### 语法检查

```bat
node\node.exe -c electron\main.js
node\node.exe -c electron\renderer\app.js
bun build plugins/claude-mode-extension.ts --no-bundle --target=bun
```

### 记忆系统验证

```bat
bun run test_memory_system.ts   # 五层架构 23 项检查
bun run test_embed.js           # embedding 单元测试
bun run test_live_memory.ts     # 实际进程端到端验证
```

### 常见坑

1. **Bun 跑 TS 需 `.ts` 后缀**：`.js` 文件里写类型注解会报语法错误
2. **PowerShell 引号**：复杂 SQL 查询写成脚本文件再 `bun run`，不要内联
3. **embedding 维度**：中文模型 512 维，英文默认 384 维，混用会导致检索失效
4. **路径硬编码**：一律用 `$env:PORTABLE_ROOT`，禁止写死 `G:/Tiffa`

---

## 八、Git 双端推送

```bat
git remote -v
# origin   → https://gitee.com/mao-yihong/oh-my-tiffa.git
# gitcode  → https://gitcode.com/weixin_42319734/Tiffa.git

git push origin master
git push gitcode master
```

**注意**：`models.yml`（含 API key）和 `data/agent/memories/` 已 gitignore，不会误提交。
